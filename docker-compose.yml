services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_ADDR=redis-db:6379
    depends_on:
      - redis-db

  redis-db:
    image: redis:alpine
    container_name: redis-db
    ports:
      - "6379:6379"